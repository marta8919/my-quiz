import { PageWrapper } from "@/components/PageWrapper";
import { Quiz } from "@/components/Quiz";

const Page = async () => {
  return (
    <PageWrapper>
      <Quiz />
    </PageWrapper>
  );
};

export default Page;
